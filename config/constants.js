const allSObjectInputsConst = {
    RESUME : ' Resume (starts where it left and overwrites anything else selected)',
    EXECUTE_ALL: ' Execute All sObject Modules (this option overwrites anything else selected)',
    GET_ALL_INVOLVED: ' Get All Involved sObject Schemas',
    REFRESH_ALL_SCHEMAS: ' Refresh All sObject Schemas From Source Org',
    QUERY_SOBJECTS:' Query sObjects From Source Org',
    BUILD_TREE: ' Build General Tree',
    LOAD_SOBJECTS:' Load sObjects To Target Org',
    ANALYSIS: ' Analysis on sObjects'
}

const particularSObjectInputsConst = {
    EXECUTE_ALL: ' Execute All sObject Modules for sObject (this option overwrites anything else selected)',
    GET_SOBJECT_SCHEMA: ' Get sObject Schema From Source Org',
    QUERY_SOBJECT: ' Query sObject From Source Org',
    LOAD_SOBJECT: ' Load sObject To Target Org',
    ANALYSIS: ' Analysis on sObject'
}

export {
    allSObjectInputsConst,
    particularSObjectInputsConst
}
